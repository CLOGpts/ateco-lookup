# ===========================================
# SYD CYBER - Backend Environment Variables
# ===========================================

# Database Configuration
# Production: Railway PostgreSQL connection string
# Development: Local PostgreSQL database
DATABASE_URL=postgresql://user:password@localhost:5432/syd_cyber

# Environment Mode
# Values: development | production
ENVIRONMENT=production

# Database Connection Pool Settings
# Numero di connessioni permanenti al database
DB_POOL_SIZE=20

# Numero massimo di connessioni extra temporanee
DB_MAX_OVERFLOW=10

# Timeout in secondi per ottenere una connessione dal pool
DB_POOL_TIMEOUT=30

# Ricicla le connessioni dopo N secondi (per evitare connessioni stale)
DB_POOL_RECYCLE=3600

# Debug Settings (solo per development)
# Log tutte le query SQL (true/false)
DB_ECHO=false

# Log attività del connection pool (true/false)
DB_ECHO_POOL=false

# Telegram Bot Configuration
# Token del bot Telegram per l'invio dei report PDF
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# ===========================================
# Notes:
# - DATABASE_URL è l'unica variabile obbligatoria
# - Le altre hanno valori di default nel codice
# - In development, imposta ENVIRONMENT=development
#   per abilitare localhost in CORS
# - TELEGRAM_BOT_TOKEN è necessario per l'invio dei pre-report via Telegram
# ===========================================
